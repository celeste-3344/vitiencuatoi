# Thư mục build tạo ra khi chạy workflow (không cần commit, GitHub Actions tự tạo lại mỗi lần deploy)
dist/

# Nếu chạy npm install để test công cụ minify ở máy
node_modules/

# Phòng trường hợp bạn tạo file config chứa key THẬT để chạy thử ở máy —
# đặt tên theo mẫu này để .gitignore tự loại, không bao giờ bị đẩy nhầm lên Git.
js/firebase-config.local.js
*.local.js

# File hệ thống
.DS_Store
Thumbs.db
